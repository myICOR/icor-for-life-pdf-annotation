export const PLUGIN_ID = 'icor-for-life-pdf-annotation';
export const PLUGIN_NAME = 'ICOR for Life - PDF Annotation';
/* Every DOM class this plugin adds starts with this. */
export const CLASS_PREFIX = 'icor-pdfa-';
/* The right-sidebar view. */
export const VIEW_TYPE = 'icor-pdf-highlights';
/* The `type` a highlight note carries in its frontmatter, and the class it
   carries in `cssclasses` so the stylesheet can tell it from other notes. */
export const NOTE_TYPE = 'pdf-highlight';
export const NOTE_CSS_CLASS = 'icor-pdf-highlight';
/* The source name handed to the drag manager for every drag this plugin
   starts. */
export const DRAG_SOURCE = 'icor-pdf-annotation';
